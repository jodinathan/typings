name: typings
description: Automated TypeScript bindings
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0-277.0.dev <4.0.0'

# Add regular dependencies here.
dependencies:
  js: ^0.6.7
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.4.1
  build_web_compilers: ^4.0.0

dependency_overrides:
  build_runner:
   path: ../../build/build_runner
